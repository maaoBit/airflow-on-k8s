aws:
  aws_access_key_id: ${AWS_ACCESS_KEY_ID:-admin}
  aws_secret_access_key: ${AWS_SECRET_ACCESS_KEY:-password1234}
  endpoint_url: ${MLFLOW_S3_ENDPOINT_URL:-http://minio.default.svc.cluster.local:9000}
