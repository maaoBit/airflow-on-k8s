apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kedro-shared-pvc
  namespace: airflow
spec:
  accessModes:
    - ReadWriteMany  # 允许多个Pod同时读写
  resources:
    requests:
      storage: 10Gi  # 根据实际需求调整存储大小
